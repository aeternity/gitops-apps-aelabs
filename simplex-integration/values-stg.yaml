app:
  ingress:
    hosts:
      - host: "simplex-integration.stg.aepps.com"
        paths:
          - path: "/"
    tls:
      - hosts:
        - "simplex-integration.stg.aepps.com"
        secretName: simplex-integration-cert
