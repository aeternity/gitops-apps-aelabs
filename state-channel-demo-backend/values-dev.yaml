app:
  ingress:
    hosts:
      - host: "state-channel-demo-backend.dev.service.aepps.com"
        paths:
          - path: "/"
      - host: "develop-state-channel-demo-backend.dev.service.aepps.com"
        paths:
          - path: "/"
            version: "develop"
  env:
    "NODE_ENV": "production"
