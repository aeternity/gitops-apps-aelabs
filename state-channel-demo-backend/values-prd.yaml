app:
  resources:
    limits:
      memory: "3Gi"
    requests:
      cpu: "1"
      memory: "2Gi"
  ingress:
    hosts:
      - host: "state-channel-demo-backend.prd.service.aepps.com"
        paths:
          - path: "/"
        version: "latest"
  env:
    "NODE_ENV": "production"
  versions:
    - {version: "latest", gitSha: "1c90e48"}
